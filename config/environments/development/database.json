{
  "defaultConnection": "default",
  "connections": {
    "default": {
      "connector": "mongoose",
      "settings": {
        "uri": "${process.env.MONGO_AMBER_RECIPE_CLUSTER0 || ''}"
      },
      "options": {
        "ssl": true
      }
    }
  },
  "database": "amber-savor-strapi-db"
}
